project_name: forge-cli
builds:
  - main: ./cmd/forge-cli
    binary: forge-cli
    goos:
      - linux
      - windows
      - darwin
    goarch:
      - amd64
      - arm64
archives:
  - format: tar.gz
